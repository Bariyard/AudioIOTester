#include "utils.h"

//double scaling(double old_min, double old_max, double new_min, double new_max, double old_value)
//{
//    return (new_max - new_min) / (old_max - old_min) * (old_value - old_min) + new_min;
//}
